import {TypographyPageTitle} from "../components/TypographyPageTitle";
import Box from "@mui/material/Box";

export const StockPage = () => {
    return (
        <Box sx={{width: 1}}>
            <TypographyPageTitle>Stocks</TypographyPageTitle>
            <div>Development is ongoing...</div>
        </Box>
    );
}
