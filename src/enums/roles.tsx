export enum Roles {
  // This must be in hierarchical order!
  // In utils is implemented method based on indexes.
  CONSULTANT = "Consultant",
  IV_STUFF = "IV stuff",
  MANAGER = "Manager",
  TENANT_ADMIN = "Tenant admin",
  SUPERADMIN = "Super admin",
}